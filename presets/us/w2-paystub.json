{
  "id": "us.w2-paystub",
  "version": 7,
  "name": "US W-2 / Pay Stub",
  "emoji": "🇺🇸",
  "description": "US tax forms and pay stubs (W-2, W-4, 1099, generic paystubs). Detects SSN, EIN, US dates, US ZIP codes, US phone numbers, bank routing numbers (context-anchored), plus names, addresses, account numbers and amounts.",
  "language": "en",
  "country": "US",
  "tags": [
    "payslip",
    "tax",
    "usa",
    "w2",
    "paystub",
    "irs",
    "compliance"
  ],
  "patterns": [
    {
      "name": "Social Security Number (SSN)",
      "regex": "\\b\\d{3}-\\d{2}-\\d{4}\\b",
      "description": "US SSN — XXX-XX-XXXX format",
      "enabled": true
    },
    {
      "name": "EIN (Employer ID)",
      "regex": "\\b\\d{2}-\\d{7}\\b",
      "description": "US Employer Identification Number — XX-XXXXXXX format",
      "enabled": true
    },
    {
      "name": "US ZIP Code",
      "regex": "\\b\\d{5}(?:-\\d{4})?\\b",
      "description": "US ZIP — 5 digits or ZIP+4 format",
      "enabled": true
    },
    {
      "name": "US date (MM/DD/YYYY)",
      "regex": "\\b(?:0[1-9]|1[0-2])/(?:0[1-9]|[12]\\d|3[01])/\\d{4}\\b",
      "description": "US date format — MM/DD/YYYY",
      "enabled": true
    },
    {
      "name": "US phone number",
      "regex": "(?<!\\d)(?:\\+1[\\s\\-.]?)?(?:\\(\\d{3}\\)[\\s\\-.]?|\\d{3}[\\s\\-.])\\d{3}[\\s\\-.]\\d{4}(?!\\d)",
      "description": "US phone with explicit formatting (parens/dashes/dots/spaces). Bare 10-digit runs excluded: they collide with account numbers",
      "enabled": true
    },
    {
      "name": "Bank routing number",
      "regex": "(?i)(?<=\\b(?:routing(?:\\s?(?:number|no\\.?|#))?|ABA|RTN)[\\s:#.\\-]{0,4})[0-9]{9}\\b",
      "description": "Routing number digits only: keyword anchors via lookbehind but stays visible",
      "enabled": true
    },
    {
      "name": "Bank account number",
      "regex": "(?i)(?<=\\b(?:account(?:\\s?(?:number|no\\.?|#))?|acct\\.?)[\\s:#.\\-]{0,4})[0-9]{6,17}\\b",
      "description": "Account number digits only: the keyword (account/acct) anchors the match via lookbehind but stays visible. Off by default: the built-in Account Numbers group already catches bare digit runs; enable this for keyword-anchored precision",
      "enabled": false
    },
    {
      "name": "Driver License (generic US)",
      "regex": "(?-i)\\b[A-Z]\\d{7,12}\\b",
      "description": "US driver license number (varies by state, generic pattern, case-sensitive)",
      "enabled": false
    }
  ],
  "builtin": {
    "email": true,
    "name": true,
    "address": true,
    "ip": false,
    "url": false,
    "amount": true,
    "accountNumber": true,
    "licensePlate": false
  }
}